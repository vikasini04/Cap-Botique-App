<!DOCTYPE html>
<html lang="en">
<head>

<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Customization</title>
<style>
   body {
            font-family: 'Lucida Handwriting', cursive;
            text-align: center;
            background: linear-gradient(rgba(67, 104, 214, 0.5), rgba(55, 163, 235, 0.5));
            color: rgb(7, 7, 7);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        h1 {
            font-size: 3rem;
            margin-bottom: 40px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
        }

        form {
            
            width: 100%;
            max-width: 70%;
            background: rgba(255, 255, 255, 0.8);
            padding: 20px;
            border-radius: 15px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        input {
            width: 100%;
            margin-bottom: 15px;
            padding: 10px;
            border-radius: 10px;
            border: 1px solid #ccc;
            font-size: 1rem;
        }

        input:focus {
            outline: none;
            border-color: #007bff;
            box-shadow: 0 0 5px rgba(0, 123, 255, 0.5);
        }

        button {
            margin: 10px;
            padding: 15px 30px;
            background-color: #0c0c0c;
            color: white;
            text-decoration: none;
            border-radius: 30px;
            font-size: 1.2rem;
            transition: background-color 0.3s ease, transform 0.3s ease;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            display: inline-block
        }

        button:hover {
            margin: 10px;
            padding: 15px 30px;
            background-color: #f1efef;
            color: rgb(18, 17, 17);
            text-decoration: none;
            border-radius: 30px;
            font-size: 1.2rem;
            transition: background-color 0.3s ease, transform 0.3s ease;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            display: inline-block;
        }
</style>
</head>
<body>
<h1>Customization</h1>
<form action="/customization/place-order" method="POST">
    <div class="form-group">
        <label for="flowerTypes">Type of Flowers</label>
        <input type="text" name="flowerTypes" class="form-control" placeholder="Type of Flowers" required>
    </div>
    <div class="form-group">
        <label for="message">Message</label>
        <input type="text" name="message" class="form-control" placeholder="Message" required>
    </div>
    <div class="form-group">
        <label for="wrapColor">Color of Wrap</label>
        <input type="text" name="wrapColor" class="form-control" placeholder="Color of Wrap" required>
    </div>
    <div class="form-group">
        <label for="totalPrice">Total Price</label>
        <input type="number" name="totalPrice" class="form-control" placeholder="Total Price" required>
    </div>
    <button type="submit" class="btn btn-primary">Place Order</button>
</form>
</body>
</html>